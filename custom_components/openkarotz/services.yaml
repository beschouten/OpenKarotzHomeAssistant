set_led:
  name: Set LED
  description: Set LED color, brightness, or preset
  fields:
    config_entry_id:
      name: Config Entry ID
      description: The ID of the OpenKarotz integration
      required: true
      example: "abc123"
    color:
      name: Color
      description: Color name (red, green, blue, yellow, cyan, magenta, white, black)
      required: false
      example: "red"
    brightness:
      name: Brightness
      description: Brightness level (0-100)
      required: false
      example: 50
    color_temperature:
      name: Color Temperature
      description: Color temperature in Kelvin
      required: false
      example: 3000
    preset:
      name: Preset
      description: Preset color scheme name
      required: false
      example: "happy"
    rgb_value:
      name: RGB Value
      description: RGB color value in hex (e.g., FF0000)
      required: false
      example: "FF0000"

play_tts:
  name: Play TTS
  description: Play text-to-speech
  fields:
    config_entry_id:
      name: Config Entry ID
      description: The ID of the OpenKarotz integration
      required: true
      example: "abc123"
    text:
      name: Text
      description: Text to speak
      required: true
      example: "Hello, this is a test"
    voice:
      name: Voice
      description: Voice identifier
      required: false
      example: "default"
    category:
      name: Category
      description: TTS category
      required: false
      example: "notification"
